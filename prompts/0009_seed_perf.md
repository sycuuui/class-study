bootRun 후 초기 데이터 생성이 1분을 넘는다.
다음 목표로 seed 성능을 개선해줘:

목표:
- 서버 시작 후 1분 이내에 seed 완료 + /health 200
- 데이터 규모는 유지(학과 10+, 교수 100+, 강좌 500+, 학생 10,000+)
- 데이터의 '현실성' 유지

제약:
- 변경 전 계획(수정 파일/핵심 변경점) 먼저 제시
- seed 성능 개선을 위해서만 수정(API 동작/응답 포맷은 유지)
- 루프 내부 로그/비싼 문자열 처리 최소화
- 컬렉션 초기 용량 설정, 사전 생성/재사용(pool) 적용

추가:
- SeedDataGenerator에 seed 단계별 소요시간(ms)을 로그로 남겨서
  어느 단계가 병목인지 확인 가능하게 해줘(단, per-item 로그 금지)

이 프롬프트 원문을 prompts/0009_seed_perf.md 로 저장해줘.

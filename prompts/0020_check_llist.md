아래 요구사항 체크리스트를 기준으로 현재 레포가 요구사항을 충족하는지 검증해줘.
1) 코드/설계/엔드포인트/문서(README 또는 API 문서)를 근거로 체크
2) 가능하면 테스트(단위/통합)와 애플리케이션 실행 후 /health 포함 API 호출로 검증
3) 항목별로 PASS/FAIL, 근거(파일 경로+핵심 코드 위치/엔드포인트/테스트 결과), 수정 제안을 표로 정리
4) 누락된 요구사항이 있으면 '가장 적은 변경'으로 충족시키는 수정 계획을 단계별로 제시

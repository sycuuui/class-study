# AGENTS.md

## 목적 (Purpose)
이 문서는 본 저장소에서 AI 에이전트(Codex, Claude, ChatGPT 등)를  
어떤 범위와 규칙으로 사용했는지를 명확히 설명하기 위한 지침서입니다.

본 프로젝트에서 AI 사용의 목적은 다음과 같습니다.
- 명확히 정의된 요구사항을 빠르고 정확하게 구현하기 위함
- 반복적이거나 기계적인 코드 작성의 부담을 줄이기 위함
- 문서(API 명세, README, 요구사항 정리) 작성을 보조하기 위함

AI는 **설계 판단이나 비즈니스 의사결정을 대체하지 않습니다.**

---

## AI 사용 허용 범위 (Allowed AI Usage)

AI 에이전트는 다음 작업을 수행할 수 있습니다.
- 이미 정의된 요구사항에 기반한 기능 구현
- 보일러플레이트 코드 및 반복 코드 생성
- 동작 변경 없는 리팩토링 보조
- 기존 로직을 기준으로 한 테스트 코드 작성
- 명시적인 요청이 있는 경우 문서 수정 및 보강

---

## AI 사용 금지 사항 (Disallowed AI Usage)

AI 에이전트는 다음을 수행해서는 안 됩니다.
- 요구사항에 없는 기능을 임의로 추가하는 행위
- API 동작, 응답 포맷, 에러 코드 체계를 임의로 변경하는 행위
- 데이터베이스, 인증, 캐시 등 외부 인프라를 새로 도입하는 행위
- 불필요한 추상화나 과도한 구조 설계
- 동시성 제어 로직 또는 비즈니스 규칙을 제거하거나 약화하는 행위
- 명시적인 요청 없이 데이터 규모(seed 크기)를 변경하는 행위

---

## 코드 작성 규칙 (Coding Rules)

AI가 생성한 코드는 다음 기준을 따라야 합니다.
- 단순하고 읽기 쉬운 Java 코드 우선
- 과도한 Stream, 복잡한 람다 사용 지양
- 동시성 로직은 명확하게 드러나야 함
- 락 획득 순서(courseId → studentId) 규칙 유지
- 에러 코드 및 HTTP 상태 코드 매핑 유지

---

## 문서 작성 규칙 (Documentation Rules)

문서를 수정할 때는 다음을 준수해야 합니다.
- 기존 문서의 스타일과 구조 유지
- 사용자가 명시한 요구사항을 요약하거나 생략하지 않음
- 구현 과정에서 발생한 암묵적 결정 사항은 명확히 문서에 남김

---

## 변경 및 커밋 절차 (Change Process)

AI 에이전트는 다음 절차를 따라야 합니다.
1. 변경 계획(수정/생성 파일 목록)을 먼저 제시
2. 사용자 승인 이후에만 코드 변경 수행
3. 승인된 범위 내에서만 작업 진행

---

## 기타
본 프로젝트에서의 최종 판단과 책임은 사람에게 있으며,  
AI는 보조 도구로만 사용됩니다.

불확실한 경우, AI는 반드시 질문 후 진행해야 합니다.